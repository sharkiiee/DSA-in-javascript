// 1. Find the number of digits in a number.

function digits(num){
    let store = num.toString();

    console.log("The number of digits in the number : " + store.length);
}

const num = 05023;

digits(num);

